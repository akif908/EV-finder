# Fix Design Artifacts — EV Charging & Battery-Swap Finder

Goal: bring the three design artifacts into full consistency with the locked project context (Kotlin Android client, no AI/ML, Spring Boot + MySQL, roles USER/OPERATOR/ADMIN, notifications module).

## 1. Rewrite the architecture diagram (`C:\Users\hp\Downloads\final aoop system design.drawio`)

The existing file is a stale Mermaid import of the old web design. I will author a clean new drawio XML (same file, new content) showing:

- **Clients**: ONE client — Kotlin Android App with role-based screens — User (Home, Map, Station Details, Booking, Payment, Bookings, Vehicles, Reviews, Notifications), Operator (Station/Service/Availability Management), Admin (Dashboard, User/Operator Management). Tech: Retrofit/OkHttp (REST), OkHttp WebSocket client.
- **Map stack (kept free)**: Kotlin Android Map screen → **osmdroid** (open-source Android map library — the Android equivalent of Leaflet) → **OpenStreetMap tile servers** (free map data, no API key, no billing). Optionally note MapLibre/Google Maps SDK as alternatives in a footnote.
- **Backend — Spring Boot (modular monolith)**: REST Controllers → Service Layer (User, Vehicle, Station, Service, Availability/Slot, Booking with double-booking prevention, Simulated Payment, Review, Notification, Admin, Operator) → Repository (Spring Data JPA/Hibernate). Plus: Spring Security + JWT + RBAC box, WebSocket server (availability/booking-status events only), @Async scheduled booking-expiry cleanup, centralized exception handling.
- **Database — MySQL (XAMPP)**: users, vehicles, stations, station_services, bookings, payments, reviews, notifications.
- **Removed**: all React.js nodes, Leaflet.js (web-only library), every AI/ML node and legend entry, AI_RECOMMENDATIONS table, "file-based persistence / system_logs.txt" box, duplicated security box.
- Keep a LEGEND and the "modular monolith, simulated payment, no AI/ML, no IoT" note (updated wording).

## 2. Corrected database schema (`D:\EV finder\schema.sql` + `D:\EV finder\schema-changes.md`)

Write a complete, runnable MySQL DDL script matching the existing diagram but fixed:

- `users`: **add `password_hash VARCHAR(255) NOT NULL`**, keep user_id/name/email(unique)/role enum/created_at.
- `stations`: add `address`, `description`, `opening_time`, `closing_time`, `created_at`, `updated_at`; lat/lng as `DECIMAL(10,7)/(11,7)`; status enum (ACTIVE/INACTIVE/TEMPORARILY_UNAVAILABLE).
- `vehicles`: add `manufacturer`, `model`, `battery_capacity_kwh` (nullable); keep type/connector/registration_no(unique).
- `station_services`: add `status` enum; keep service_type enum, connector_type, price_per_unit, available_slots (Option B capacity model).
- `bookings`: add `created_at`; keep FKs to user/vehicle/station/service, start/end timestamps, status enum (PENDING/CONFIRMED/COMPLETED/CANCELLED). Add index on (service_id, start_time, end_time) for conflict checks.
- `payments`: keep 1:1 via unique booking_id FK; add `created_at`; status enum (PENDING/SUCCESS/FAILED/REFUNDED), method enum.
- `reviews`: keep unique booking_id; add `created_at`; rating INT with CHECK (1–5).
- **New `notifications` table**: notification_id PK, user_id FK, title, message, type enum, is_read boolean, created_at.
- FKs with proper ON DELETE behavior, indexes on all FK columns.
- `schema-changes.md` briefly lists each change vs the PNG diagram so you can update the dbdiagram.io source too.

## 3. Make the backend scaffold runnable (`D:\EV finder\EV-finder-api`)

- `pom.xml`: add `jjwt-api/impl/jackson` (JWT) dependency; keep everything else.
- `application.properties`: MySQL datasource URL (localhost:3306/ev_finder, XAMPP), JPA `ddl-auto=update`, SQL logging, JWT secret/expiry placeholders, server port 8080.
- Create the package skeleton (config, controller, service, service.impl, repository, entity, dto, mapper, security, websocket, exception, util) with a README noting the phase order (auth first).
- No business logic yet — this step only makes `mvn spring-boot:run` start cleanly against XAMPP MySQL and sets up structure for Phase 2 (Authentication).

## 4. Verification

- Validate the drawio XML is well-formed (open-able in draw.io).
- Run the DDL against local MySQL if XAMPP is running (otherwise hand off the exact command).
- `mvn -q compile` / `spring-boot:run` in EV-finder-api to confirm the scaffold starts.

UI design review will follow when you resend the screens as smaller images.