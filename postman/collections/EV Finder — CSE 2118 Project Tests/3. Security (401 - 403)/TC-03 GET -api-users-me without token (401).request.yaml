$kind: http-request
name: TC-03 GET /api/users/me without token (401)
url: "{{baseUrl}}/api/users/me"
method: GET
headers:
  Content-Type: application/json
scripts:
  - type: afterResponse
    code: pm.test('TC-03 status is 401', () => pm.response.to.have.status(401));
    language: text/javascript
order: 1000
