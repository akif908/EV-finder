$kind: http-request
url: "{{baseUrl}}/api/bookings/{{bookingId}}/cancel"
method: PUT
headers:
  Authorization: Bearer {{userToken}}
  Content-Type: application/json
scripts:
  - type: afterResponse
    code: pm.test('TC-49 status is 400', () => pm.response.to.have.status(400));
    language: text/javascript
order: 7000
