$kind: http-request
url: "{{baseUrl}}/api/bookings/{{bookingId}}/cancel"
method: PUT
headers:
  Authorization: Bearer {{userToken}}
  Content-Type: application/json
scripts:
  - type: afterResponse
    code: pm.test('TC-48 status is 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 6000
