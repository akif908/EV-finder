$kind: http-request
url: "{{baseUrl}}/api/notifications/unread-count"
method: GET
headers:
  Authorization: Bearer {{userToken}}
  Content-Type: application/json
scripts:
  - type: afterResponse
    code: pm.test('TC-31 status is 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 2000
