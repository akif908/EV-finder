$kind: http-request
name: TC-20 GET /api/bookings/my (200)
url: "{{baseUrl}}/api/bookings/my"
method: GET
headers:
  Authorization: Bearer {{userToken}}
  Content-Type: application/json
scripts:
  - type: afterResponse
    code: pm.test('TC-20 status is 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 9000
