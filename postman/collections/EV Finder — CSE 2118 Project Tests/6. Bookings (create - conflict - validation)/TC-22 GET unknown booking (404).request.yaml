$kind: http-request
url: "{{baseUrl}}/api/bookings/does-not-exist"
method: GET
headers:
  Authorization: Bearer {{userToken}}
  Content-Type: application/json
scripts:
  - type: afterResponse
    code: pm.test('TC-22 status is 404', () => pm.response.to.have.status(404));
    language: text/javascript
order: 7000
