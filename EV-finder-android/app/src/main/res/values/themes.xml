<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose app; this theme only styles the window before Compose draws. -->
    <style name="Theme.EVFinder" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/ev_background</item>
        <item name="android:statusBarColor">@color/ev_background</item>
    </style>
</resources>
